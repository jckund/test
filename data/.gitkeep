Files in this directory are generated by scraper.py on each scheduled run:
  latest.json    full raw Kalshi API response
  snapshot.json  normalized current market state
  history.jsonl  append-only, one record per run that had changes
  CHANGES.md     human-readable change log, newest first
